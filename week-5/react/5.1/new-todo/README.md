## Todo application

This project contains a simple TODO application

- A user can sign up
- A user can sign in
- A user can create a todo
- A user can see their exisiting todos
- A user can mark their todos as done

// initialize a node package
// go to backend and then run npm init -y

//npm install express
node_modules downloaded


"dependencies": {
    "express": "^4.21.2"
}

now create the basic routes in express
create the types page to check types